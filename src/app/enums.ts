export enum SeasonType {
  Postseason = 'postseason',
  Preseason = 'preseason',
  Regular = 'regular',
}

export enum GameStatus {
  Scheduled = 'scheduled',
  InProgress = 'in_progress',
  Final = 'final',
  Postponed = 'postponed',
  Cancelled = 'cancelled',
}
